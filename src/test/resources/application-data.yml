spring:
  config:
    activate:
      on-profile: data
  datasource:
    url: jdbc:mysql://54.180.75.220:3306/flow?rewriteBatchedStatements=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

